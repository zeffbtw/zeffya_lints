name: zeffya_lints
description: "Custom lints"
version: 1.0.0
homepage:

environment:
  sdk: ">=3.4.3 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  custom_lint_builder:
  analyzer:
  analyzer_plugin:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  custom_lint:

flutter:
